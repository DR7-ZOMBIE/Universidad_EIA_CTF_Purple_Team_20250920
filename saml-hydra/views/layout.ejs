<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head') %>
  <body class="min-h-screen bg-hydra text-white">
    <header class="px-6 lg:px-10 py-6">
      <div class="max-w-6xl mx-auto flex items-center justify-between">
        <a href="/" class="flex items-center gap-3">
          <img src="/static/hydra.svg" class="w-9 h-9" alt="hydra" />
          <div class="text-xl font-semibold tracking-wide">SAML Hydra</div>
        </a>
        <nav class="hidden md:flex items-center gap-6 text-sm text-white/80">
          <a class="hover:text-white" href="/">Inicio</a>
          <a class="hover:text-white" href="/about">Acerca</a>
          <% if (user) { %>
          <a class="hover:text-white" href="/dashboard">Panel</a>
          <% } %>
          <a
            class="px-3 py-1.5 rounded-lg glass hover:bg-white/10"
            href="/static/certs/sp_public.pem"
            >SP Public Key</a
          >
        </nav>
      </div>
    </header>

    <main class="px-6 lg:px-10">
      <div class="max-w-6xl mx-auto"><%- body %></div>
    </main>

    <footer class="mt-16 py-10 text-center text-white/50 text-sm">
      Many heads sign, but only one truth escapes.
    </footer>
  </body>
</html>
